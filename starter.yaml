services:
  composer:
    pre-commands:
      - "composer config github.com mamau some-token"
#    config:
#      repository:
#        url: "github.com"
#        username: "mamau"
#        token: "some-token"
  yarn:
    dns:
      - "8.8.8.8"
      - "8.8.4.4"
    pre-commands:
      - "yarn config set strict-ssl false"
      - "npm config set"

#//TODO: сделать последовательный запуск команд
#commands:
#  init:
#    - "yarn install"
#    - "composer --version=1.9 i"
#  build:
#    - "bower i"