commands:
  composer:
    version: "2"
    user-id: "1000"
  bower:
    user-id: "1000"
  yarn:
    version: "10"
    user-id: "1000"

macros:
  - test:
    name: "test"
    commands:
      - "yarn install"
      - "composer install --ignore-platform-reqs"
  - test2:
    name: "test2"
    commands:
      - "yarn install"

services:
  - php:
    name: "php"
    version: "7.4"
    client-command: "php"
  - mysql:
    name: "mysql"
    version: "5.7"
