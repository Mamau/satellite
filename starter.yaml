services:
  - name: "php"
    version: "7.4"
    image-command: "php"
    pre-commands:
      - "php -v"
    post-commands:
      - "php -v"
  - name: "mysql"
    version: "5.7"

macros:
  - test:
    name: "test"
    commands:
      - "php -v"
      - "mysql -v"
  - test2:
    name: "test2"
    commands:
      - "php -v"
