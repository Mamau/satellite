services:
  - name: "php"
    version: "7.4"
  - name: "mysql"
    version: "5.7"

macros:
  - test:
    name: "test"
    commands:
      - "yarn install"
      - "composer install --ignore-platform-reqs"
  - test2:
    name: "test2"
    commands:
      - "yarn install"
